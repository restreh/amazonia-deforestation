# Entorno reproducible del proyecto amazonia-deforestation.
# Python 3.12 recomendado. Instalacion: pip install -r requirements.txt
# El tablero Streamlit tiene su propio archivo de dependencias en
# dashboards/streamlit/requirements.txt para despliegue independiente.

# --- Nucleo numerico y de datos ---
numpy>=1.26,<2.1
scipy>=1.11
pandas>=2.1
pyyaml>=6.0

# --- Geoespacial: IO y vectores ---
rasterio>=1.3.9
rioxarray>=0.15
xarray>=2024.1
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
fiona>=1.9

# --- STAC, cubos de datos perezosos y procesamiento distribuido ---
pystac-client>=0.7
stackstac>=0.5
dask[complete]>=2024.1

# --- Almacenamiento columnar y acceso a AWS ---
pyarrow>=15.0
boto3>=1.34
s3fs>=2024.2

# --- Aprendizaje automatico: baselines ---
scikit-learn>=1.4
xgboost>=2.0

# --- Aprendizaje profundo: U-Net ---
torch>=2.2
torchvision>=0.17
segmentation-models-pytorch>=0.3

# --- Registro de experimentos ---
mlflow>=2.10

# --- Visualizacion y tablero interactivo ---
streamlit>=1.31
streamlit-folium>=0.20
leafmap>=0.30
folium>=0.15
plotly>=5.18
matplotlib>=3.8
